fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
python-dotenv>=1.0
opencv-python>=4.9
ultralytics>=8.2
numpy>=1.26

# --- VideoMAE temporal action recognition dependencies ---
# torch & torchvision: CPU wheel by default. For CUDA GPU acceleration install
# the CUDA build from https://pytorch.org/get-started/locally/ (the app still
# runs on CPU if CUDA is unavailable and automatically uses the GPU when present).
torch>=2.1,<3.0
torchvision>=0.16,<1.0
transformers>=4.38,<5.0
accelerate>=0.28,<2.0
av>=11,<13
